export const EXCHANGE_RATES: Record<string, { name: string; symbol: string }> = {
  AED: { name: "UAE Dirham", symbol: "د.إ" },
  AFN: { name: "Afghan Afghani", symbol: "؋" },
  ALL: { name: "Albanian Lek", symbol: "L" },
  AMD: { name: "Armenian Dram", symbol: "֏" },
  ANG: { name: "Netherlands Antillean Guilder", symbol: "ƒ" },
  AOA: { name: "Angolan Kwanza", symbol: "Kz" },
  ARS: { name: "Argentine Peso", symbol: "$" },
  AUD: { name: "Australian Dollar", symbol: "A$" },
  AWG: { name: "Aruban Florin", symbol: "ƒ" },
  AZN: { name: "Azerbaijani Manat", symbol: "₼" },
  BAM: { name: "Bosnia-Herzegovina Convertible Mark", symbol: "KM" },
  BBD: { name: "Barbadian Dollar", symbol: "Bds$" },
  BDT: { name: "Bangladeshi Taka", symbol: "৳" },
  BGN: { name: "Bulgarian Lev", symbol: "лв" },
  BHD: { name: "Bahraini Dinar", symbol: ".د.ب" },
  BIF: { name: "Burundian Franc", symbol: "FBu" },
  BMD: { name: "Bermudian Dollar", symbol: "$" },
  BND: { name: "Brunei Dollar", symbol: "B$" },
  BOB: { name: "Bolivian Boliviano", symbol: "Bs." },
  BRL: { name: "Brazilian Real", symbol: "R$" },
  BSD: { name: "Bahamian Dollar", symbol: "$" },
  BTN: { name: "Bhutanese Ngultrum", symbol: "Nu." },
  BWP: { name: "Botswanan Pula", symbol: "P" },
  BYN: { name: "Belarusian Ruble", symbol: "Br" },
  BZD: { name: "Belize Dollar", symbol: "BZ$" },
  CAD: { name: "Canadian Dollar", symbol: "C$" },
  CDF: { name: "Congolese Franc", symbol: "FC" },
  CHF: { name: "Swiss Franc", symbol: "CHF" },
  CLP: { name: "Chilean Peso", symbol: "$" },
  CNY: { name: "Chinese Yuan", symbol: "¥" },
  COP: { name: "Colombian Peso", symbol: "$" },
  CRC: { name: "Costa Rican Colón", symbol: "₡" },
  CUP: { name: "Cuban Peso", symbol: "$" },
  CVE: { name: "Cape Verdean Escudo", symbol: "$" },
  CZK: { name: "Czech Koruna", symbol: "Kč" },
  DJF: { name: "Djiboutian Franc", symbol: "Fdj" },
  DKK: { name: "Danish Krone", symbol: "kr" },
  DOP: { name: "Dominican Peso", symbol: "RD$" },
  DZD: { name: "Algerian Dinar", symbol: "د.ج" },
  EGP: { name: "Egyptian Pound", symbol: "E£" },
  ERN: { name: "Eritrean Nakfa", symbol: "Nkf" },
  ETB: { name: "Ethiopian Birr", symbol: "Br" },
  EUR: { name: "Euro", symbol: "€" },
  FJD: { name: "Fijian Dollar", symbol: "FJ$" },
  FKP: { name: "Falkland Islands Pound", symbol: "£" },
  FOK: { name: "Faroese Króna", symbol: "kr" },
  GBP: { name: "British Pound Sterling", symbol: "£" },
  GEL: { name: "Georgian Lari", symbol: "₾" },
  GGP: { name: "Guernsey Pound", symbol: "£" },
  GHS: { name: "Ghanaian Cedi", symbol: "GH₵" },
  GIP: { name: "Gibraltar Pound", symbol: "£" },
  GMD: { name: "Gambian Dalasi", symbol: "D" },
  GNF: { name: "Guinean Franc", symbol: "FG" },
  GTQ: { name: "Guatemalan Quetzal", symbol: "Q" },
  GYD: { name: "Guyanaese Dollar", symbol: "G$" },
  HKD: { name: "Hong Kong Dollar", symbol: "HK$" },
  HNL: { name: "Honduran Lempira", symbol: "L" },
  HRK: { name: "Croatian Kuna", symbol: "kn" },
  HTG: { name: "Haitian Gourde", symbol: "G" },
  HUF: { name: "Hungarian Forint", symbol: "Ft" },
  IDR: { name: "Indonesian Rupiah", symbol: "Rp" },
  ILS: { name: "Israeli New Shekel", symbol: "₪" },
  IMP: { name: "Isle of Man Pound", symbol: "£" },
  INR: { name: "Indian Rupee", symbol: "₹" },
  IQD: { name: "Iraqi Dinar", symbol: "ع.د" },
  IRR: { name: "Iranian Rial", symbol: "﷼" },
  ISK: { name: "Icelandic Króna", symbol: "kr" },
  JEP: { name: "Jersey Pound", symbol: "£" },
  JMD: { name: "Jamaican Dollar", symbol: "J$" },
  JOD: { name: "Jordanian Dinar", symbol: "د.ا" },
  JPY: { name: "Japanese Yen", symbol: "¥" },
  KES: { name: "Kenyan Shilling", symbol: "KSh" },
  KGS: { name: "Kyrgystani Som", symbol: "с" },
  KHR: { name: "Cambodian Riel", symbol: "៛" },
  KID: { name: "Kiribati Dollar", symbol: "$" },
  KMF: { name: "Comorian Franc", symbol: "CF" },
  KRW: { name: "South Korean Won", symbol: "₩" },
  KWD: { name: "Kuwaiti Dinar", symbol: "د.ك" },
  KYD: { name: "Cayman Islands Dollar", symbol: "$" },
  KZT: { name: "Kazakhstani Tenge", symbol: "₸" },
  LAK: { name: "Laotian Kip", symbol: "₭" },
  LBP: { name: "Lebanese Pound", symbol: "ل.ل" },
  LKR: { name: "Sri Lankan Rupee", symbol: "Rs" },
  LRD: { name: "Liberian Dollar", symbol: "$" },
  LSL: { name: "Lesotho Loti", symbol: "M" },
  LYD: { name: "Libyan Dinar", symbol: "ل.د" },
  MAD: { name: "Moroccan Dirham", symbol: "د.م." },
  MDL: { name: "Moldovan Leu", symbol: "L" },
  MGA: { name: "Malagasy Ariary", symbol: "Ar" },
  MKD: { name: "Macedonian Denar", symbol: "ден" },
  MMK: { name: "Myanmar Kyat", symbol: "Ks" },
  MNT: { name: "Mongolian Tugrik", symbol: "₮" },
  MOP: { name: "Macanese Pataca", symbol: "MOP$" },
  MRU: { name: "Mauritanian Ouguiya", symbol: "UM" },
  MUR: { name: "Mauritian Rupee", symbol: "₨" },
  MVR: { name: "Maldivian Rufiyaa", symbol: "Rf" },
  MWK: { name: "Malawian Kwacha", symbol: "MK" },
  MXN: { name: "Mexican Peso", symbol: "Mex$" },
  MYR: { name: "Malaysian Ringgit", symbol: "RM" },
  MZN: { name: "Mozambican Metical", symbol: "MT" },
  NAD: { name: "Namibian Dollar", symbol: "N$" },
  NGN: { name: "Nigerian Naira", symbol: "₦" },
  NIO: { name: "Nicaraguan Córdoba", symbol: "C$" },
  NOK: { name: "Norwegian Krone", symbol: "kr" },
  NPR: { name: "Nepalese Rupee", symbol: "₨" },
  NZD: { name: "New Zealand Dollar", symbol: "NZ$" },
  OMR: { name: "Omani Rial", symbol: "ر.ع." },
  PAB: { name: "Panamanian Balboa", symbol: "B/." },
  PEN: { name: "Peruvian Sol", symbol: "S/" },
  PGK: { name: "Papua New Guinean Kina", symbol: "K" },
  PHP: { name: "Philippine Peso", symbol: "₱" },
  PKR: { name: "Pakistani Rupee", symbol: "₨" },
  PLN: { name: "Polish Zloty", symbol: "zł" },
  PYG: { name: "Paraguayan Guarani", symbol: "₲" },
  QAR: { name: "Qatari Rial", symbol: "ر.ق" },
  RON: { name: "Romanian Leu", symbol: "lei" },
  RSD: { name: "Serbian Dinar", symbol: "дин" },
  RUB: { name: "Russian Ruble", symbol: "₽" },
  RWF: { name: "Rwandan Franc", symbol: "FRw" },
  SAR: { name: "Saudi Riyal", symbol: "﷼" },
  SBD: { name: "Solomon Islands Dollar", symbol: "SI$" },
  SCR: { name: "Seychellois Rupee", symbol: "₨" },
  SDG: { name: "Sudanese Pound", symbol: "ج.س." },
  SEK: { name: "Swedish Krona", symbol: "kr" },
  SGD: { name: "Singapore Dollar", symbol: "S$" },
  SHP: { name: "Saint Helena Pound", symbol: "£" },
  SLE: { name: "Sierra Leonean Leone", symbol: "Le" },
  SOS: { name: "Somali Shilling", symbol: "Sh" },
  SRD: { name: "Surinamese Dollar", symbol: "$" },
  SSP: { name: "South Sudanese Pound", symbol: "£" },
  STN: { name: "São Tomé and Príncipe Dobra", symbol: "Db" },
  SYP: { name: "Syrian Pound", symbol: "£S" },
  SZL: { name: "Swazi Lilangeni", symbol: "E" },
  THB: { name: "Thai Baht", symbol: "฿" },
  TJS: { name: "Tajikistani Somoni", symbol: "ЅМ" },
  TMT: { name: "Turkmenistan Manat", symbol: "m" },
  TND: { name: "Tunisian Dinar", symbol: "د.ت" },
  TOP: { name: "Tongan Pa'anga", symbol: "T$" },
  TRY: { name: "Turkish Lira", symbol: "₺" },
  TTD: { name: "Trinidad and Tobago Dollar", symbol: "TT$" },
  TVD: { name: "Tuvaluan Dollar", symbol: "$" },
  TWD: { name: "New Taiwan Dollar", symbol: "NT$" },
  TZS: { name: "Tanzanian Shilling", symbol: "TSh" },
  UAH: { name: "Ukrainian Hryvnia", symbol: "₴" },
  UGX: { name: "Ugandan Shilling", symbol: "USh" },
  USD: { name: "United States Dollar", symbol: "$" },
  UYU: { name: "Uruguayan Peso", symbol: "$U" },
  UZS: { name: "Uzbekistani Som", symbol: "so'm" },
  VES: { name: "Venezuelan Bolívar", symbol: "Bs." },
  VND: { name: "Vietnamese Dong", symbol: "₫" },
  VUV: { name: "Vanuatu Vatu", symbol: "VT" },
  WST: { name: "Samoan Tala", symbol: "WS$" },
  XAF: { name: "Central African CFA Franc", symbol: "FCFA" },
  XCD: { name: "East Caribbean Dollar", symbol: "EC$" },
  XOF: { name: "West African CFA Franc", symbol: "CFA" },
  XPF: { name: "CFP Franc", symbol: "₣" },
  YER: { name: "Yemeni Rial", symbol: "﷼" },
  ZAR: { name: "South African Rand", symbol: "R" },
  ZMW: { name: "Zambian Kwacha", symbol: "ZK" },
  ZWL: { name: "Zimbabwean Dollar", symbol: "Z$" },
};

export const CURRENCY_SYMBOLS: Record<string, string> = {
  AED: "د.إ", AFN: "؋", ALL: "L", AMD: "֏", ANG: "ƒ", AOA: "Kz", ARS: "$", AUD: "A$",
  AWG: "ƒ", AZN: "₼", BAM: "KM", BBD: "Bds$", BDT: "৳", BGN: "лв", BHD: ".د.ب",
  BIF: "FBu", BMD: "$", BND: "B$", BOB: "Bs.", BRL: "R$", BSD: "$", BTN: "Nu.",
  BWP: "P", BYN: "Br", BZD: "BZ$", CAD: "C$", CDF: "FC", CHF: "CHF", CLP: "$",
  CNY: "¥", COP: "$", CRC: "₡", CUP: "$", CVE: "$", CZK: "Kč", DJF: "Fdj",
  DKK: "kr", DOP: "RD$", DZD: "د.ج", EGP: "E£", ERN: "Nkf", ETB: "Br", EUR: "€",
  FJD: "FJ$", FKP: "£", FOK: "kr", GBP: "£", GEL: "₾", GGP: "£", GHS: "GH₵",
  GIP: "£", GMD: "D", GNF: "FG", GTQ: "Q", GYD: "G$", HKD: "HK$", HNL: "L",
  HRK: "kn", HTG: "G", HUF: "Ft", IDR: "Rp", ILS: "₪", IMP: "£", INR: "₹",
  IQD: "ع.د", IRR: "﷼", ISK: "kr", JEP: "£", JMD: "J$", JOD: "د.ا", JPY: "¥",
  KES: "KSh", KGS: "с", KHR: "៛", KID: "$", KMF: "CF", KRW: "₩", KWD: "د.ك",
  KYD: "$", KZT: "₸", LAK: "₭", LBP: "ل.ل", LKR: "Rs", LRD: "$", LSL: "M",
  LYD: "ل.د", MAD: "د.م.", MDL: "L", MGA: "Ar", MKD: "ден", MMK: "Ks", MNT: "₮",
  MOP: "MOP$", MRU: "UM", MUR: "₨", MVR: "Rf", MWK: "MK", MXN: "Mex$", MYR: "RM",
  MZN: "MT", NAD: "N$", NGN: "₦", NIO: "C$", NOK: "kr", NPR: "₨", NZD: "NZ$",
  OMR: "ر.ع.", PAB: "B/.", PEN: "S/", PGK: "K", PHP: "₱", PKR: "₨", PLN: "zł",
  PYG: "₲", QAR: "ر.ق", RON: "lei", RSD: "дин", RUB: "₽", RWF: "FRw", SAR: "﷼",
  SBD: "SI$", SCR: "₨", SDG: "ج.س.", SEK: "kr", SGD: "S$", SHP: "£", SLE: "Le",
  SOS: "Sh", SRD: "$", SSP: "£", STN: "Db", SYP: "£S", SZL: "E", THB: "฿",
  TJS: "ЅМ", TMT: "m", TND: "د.ت", TOP: "T$", TRY: "₺", TTD: "TT$", TVD: "$",
  TWD: "NT$", TZS: "TSh", UAH: "₴", UGX: "USh", USD: "$", UYU: "$U", UZS: "so'm",
  VES: "Bs.", VND: "₫", VUV: "VT", WST: "WS$", XAF: "FCFA", XCD: "EC$",
  XOF: "CFA", XPF: "₣", YER: "﷼", ZAR: "R", ZMW: "ZK", ZWL: "Z$",
};

export const COUNTRY_CODES: Record<string, string> = {
  "Afghanistan": "AF", "Albania": "AL", "Algeria": "DZ", "Andorra": "AD", "Angola": "AO",
  "Argentina": "AR", "Armenia": "AM", "Australia": "AU", "Austria": "AT", "Azerbaijan": "AZ",
  "Bahamas": "BS", "Bahrain": "BH", "Bangladesh": "BD", "Barbados": "BB", "Belarus": "BY",
  "Belgium": "BE", "Belize": "BZ", "Benin": "BJ", "Bhutan": "BT", "Bolivia": "BO",
  "Bosnia and Herzegovina": "BA", "Botswana": "BW", "Brazil": "BR", "Brunei": "BN",
  "Bulgaria": "BG", "Burkina Faso": "BF", "Burundi": "BI", "Cambodia": "KH", "Cameroon": "CM",
  "Canada": "CA", "Chad": "TD", "Chile": "CL", "China": "CN", "Colombia": "CO",
  "Costa Rica": "CR", "Croatia": "HR", "Cuba": "CU", "Cyprus": "CY", "Czech Republic": "CZ",
  "Denmark": "DK", "Djibouti": "DJ", "Dominican Republic": "DO", "Ecuador": "EC", "Egypt": "EG",
  "El Salvador": "SV", "Eritrea": "ER", "Estonia": "EE", "Ethiopia": "ET", "Fiji": "FJ",
  "Finland": "FI", "France": "FR", "Gabon": "GA", "Gambia": "GM", "Georgia": "GE",
  "Germany": "DE", "Ghana": "GH", "Greece": "GR", "Guatemala": "GT", "Guinea": "GN",
  "Haiti": "HT", "Honduras": "HN", "Hungary": "HU", "Iceland": "IS", "India": "IN",
  "Indonesia": "ID", "Iran": "IR", "Iraq": "IQ", "Ireland": "IE", "Israel": "IL",
  "Italy": "IT", "Jamaica": "JM", "Japan": "JP", "Jordan": "JO", "Kazakhstan": "KZ",
  "Kenya": "KE", "Kuwait": "KW", "Kyrgyzstan": "KG", "Laos": "LA", "Latvia": "LV",
  "Lebanon": "LB", "Libya": "LY", "Lithuania": "LT", "Luxembourg": "LU", "Madagascar": "MG",
  "Malaysia": "MY", "Maldives": "MV", "Mali": "ML", "Malta": "MT", "Mauritania": "MR",
  "Mauritius": "MU", "Mexico": "MX", "Moldova": "MD", "Monaco": "MC", "Mongolia": "MN",
  "Montenegro": "ME", "Morocco": "MA", "Mozambique": "MZ", "Myanmar": "MM", "Namibia": "NA",
  "Nepal": "NP", "Netherlands": "NL", "New Zealand": "NZ", "Nicaragua": "NI", "Niger": "NE",
  "Nigeria": "NG", "North Korea": "KP", "Norway": "NO", "Oman": "OM", "Pakistan": "PK",
  "Palestine": "PS", "Panama": "PA", "Paraguay": "PY", "Peru": "PE", "Philippines": "PH",
  "Poland": "PL", "Portugal": "PT", "Qatar": "QA", "Romania": "RO", "Russia": "RU",
  "Rwanda": "RW", "Saudi Arabia": "SA", "Senegal": "SN", "Serbia": "RS", "Singapore": "SG",
  "Slovakia": "SK", "Slovenia": "SI", "Somalia": "SO", "South Africa": "ZA", "South Korea": "KR",
  "Spain": "ES", "Sri Lanka": "LK", "Sudan": "SD", "Sweden": "SE", "Switzerland": "CH",
  "Syria": "SY", "Taiwan": "TW", "Tajikistan": "TJ", "Tanzania": "TZ", "Thailand": "TH",
  "Tunisia": "TN", "Turkey": "TR", "Turkmenistan": "TM", "Uganda": "UG", "Ukraine": "UA",
  "United Arab Emirates": "AE", "United Kingdom": "GB", "United States": "US", "Uruguay": "UY",
  "Uzbekistan": "UZ", "Venezuela": "VE", "Vietnam": "VN", "Yemen": "YE", "Zambia": "ZM",
  "Zimbabwe": "ZW",
};

export function formatCurrency(amount: number | string | unknown, currency?: string, locale?: string): string {
  const curr = currency ?? "EGP";
  const parsed = typeof amount === "number" ? amount : parseFloat(String(amount ?? 0));
  const num = !isNaN(parsed) ? parsed : 0;
  try {
    if (curr === "EGP") {
      const isArabic = !locale || locale.startsWith("ar");
      return isArabic
        ? `${num.toLocaleString("en-US", { minimumFractionDigits: 2, maximumFractionDigits: 2 })} ج.م`
        : `${num.toLocaleString("en-US", { minimumFractionDigits: 2, maximumFractionDigits: 2 })} EGP`;
    }
    return new Intl.NumberFormat(locale ?? "en-US", {
      style: "currency",
      currency: curr,
      minimumFractionDigits: 2,
      maximumFractionDigits: 2,
    }).format(num);
  } catch {
    const symbol = (CURRENCY_SYMBOLS[curr]) ? CURRENCY_SYMBOLS[curr] : (curr === "EGP" ? "ج.م" : "$");
    return `${num.toFixed(2)} ${symbol}`;
  }
}

export function formatDate(date: Date | string, format?: string, locale?: string): string {
  const d = typeof date === "string" ? new Date(date) : date;
  if (isNaN(d.getTime())) return "Invalid Date";

  if (!format) {
    return new Intl.DateTimeFormat(locale ?? "en-US", {
      year: "numeric",
      month: "2-digit",
      day: "2-digit",
    }).format(d);
  }

  const pad = (n: number) => n.toString().padStart(2, "0");
  const replacements: Record<string, string> = {
    YYYY: d.getFullYear().toString(),
    YY: d.getFullYear().toString().slice(-2),
    MM: pad(d.getMonth() + 1),
    DD: pad(d.getDate()),
    HH: pad(d.getHours()),
    mm: pad(d.getMinutes()),
    ss: pad(d.getSeconds()),
    M: (d.getMonth() + 1).toString(),
    D: d.getDate().toString(),
  };

  let result = format;
  for (const [key, value] of Object.entries(replacements)) {
    result = result.replace(key, value);
  }
  return result;
}

export function formatDateTime(date: Date | string, locale?: string): string {
  const d = typeof date === "string" ? new Date(date) : date;
  if (isNaN(d.getTime())) return "Invalid Date";
  return new Intl.DateTimeFormat(locale ?? "en-US", {
    year: "numeric",
    month: "2-digit",
    day: "2-digit",
    hour: "2-digit",
    minute: "2-digit",
    second: "2-digit",
    hour12: false,
  }).format(d);
}

export function generateInvoiceNumber(type: string): string {
  const now = new Date();
  const year = now.getFullYear().toString();
  const random = Math.floor(Math.random() * 1000000)
    .toString()
    .padStart(6, "0");
  const prefix = type.toUpperCase().replace(/[^A-Z]/g, "").slice(0, 6) || "INV";
  return `${prefix}-${year}-${random}`;
}

export function generateSKU(): string {
  const chars = "ABCDEFGHIJKLMNOPQRSTUVWXYZ0123456789";
  const segment = (len: number) => {
    let s = "";
    for (let i = 0; i < len; i++) {
      s += chars[Math.floor(Math.random() * chars.length)] ?? "";
    }
    return s;
  };
  return `${segment(4)}-${segment(3)}-${segment(3)}`;
}

export function generateBarcode(): string {
  const digits: number[] = [];
  for (let i = 0; i < 12; i++) {
    digits.push(Math.floor(Math.random() * 10));
  }
  let sum = 0;
  for (let i = 0; i < 12; i++) {
    sum += (digits[i] ?? 0) * (i % 2 === 0 ? 1 : 3);
  }
  const checkDigit = (10 - (sum % 10)) % 10;
  digits.push(checkDigit);
  return digits.join("");
}

export function generateCouponCode(length?: number): string {
  const len = length ?? 10;
  const chars = "ABCDEFGHIJKLMNOPQRSTUVWXYZ0123456789";
  let code = "";
  for (let i = 0; i < len; i++) {
    code += chars[Math.floor(Math.random() * chars.length)] ?? "";
  }
  return code;
}

export function generateGiftCardCode(): string {
  const chars = "0123456789";
  const block = (len: number) => {
    let s = "";
    for (let i = 0; i < len; i++) {
      s += chars[Math.floor(Math.random() * chars.length)] ?? "";
    }
    return s;
  };
  return `${block(4)}-${block(4)}-${block(4)}-${block(4)}`;
}

export function generateUUID(): string {
  return "xxxxxxxx-xxxx-4xxx-yxxx-xxxxxxxxxxxx".replace(/[xy]/g, (c) => {
    const r = (Math.random() * 16) | 0;
    const v = c === "x" ? r : (r & 0x3) | 0x8;
    return v.toString(16);
  });
}

export function calculateTax(amount: number, rate: number): number {
  return roundToTwo(amount * (rate / 100));
}

export function calculateDiscount(amount: number, discountPercent: number): number {
  return roundToTwo(amount * (discountPercent / 100));
}

export function calculateTotal(
  items: { quantity: number; unitPrice: number; discount: number; taxRate: number }[]
): { subtotal: number; totalDiscount: number; totalTax: number; grandTotal: number } {
  let subtotal = 0;
  let totalDiscount = 0;
  let totalTax = 0;

  for (const item of items) {
    const lineTotal = roundToTwo(item.quantity * item.unitPrice);
    subtotal += lineTotal;
    const discount = roundToTwo(lineTotal * (item.discount / 100));
    totalDiscount += discount;
    const taxable = roundToTwo(lineTotal - discount);
    totalTax += roundToTwo(taxable * (item.taxRate / 100));
  }

  subtotal = roundToTwo(subtotal);
  totalDiscount = roundToTwo(totalDiscount);
  totalTax = roundToTwo(totalTax);
  const grandTotal = roundToTwo(subtotal - totalDiscount + totalTax);

  return { subtotal, totalDiscount, totalTax, grandTotal };
}

export function calculateChange(paid: number, total: number): number {
  return roundToTwo(paid - total);
}

export function calculateLoyaltyPoints(amount: number, pointsPerCurrency: number): number {
  return Math.floor(amount * pointsPerCurrency);
}

export function calculateRedemptionValue(points: number, redemptionRate: number): number {
  return roundToTwo(points / redemptionRate);
}

export function roundToTwo(amount: number): number {
  return Math.round(amount * 100) / 100;
}

export function clamp(value: number, min: number, max: number): number {
  return Math.min(Math.max(value, min), max);
}

export function debounce<T extends (...args: any[]) => any>(
  fn: T,
  delay: number
): (...args: Parameters<T>) => void {
  let timeoutId: ReturnType<typeof setTimeout> | null = null;
  return (...args: Parameters<T>) => {
    if (timeoutId !== null) {
      clearTimeout(timeoutId);
    }
    timeoutId = setTimeout(() => {
      fn(...args);
      timeoutId = null;
    }, delay);
  };
}

export function throttle<T extends (...args: any[]) => any>(
  fn: T,
  limit: number
): (...args: Parameters<T>) => void {
  let inThrottle = false;
  return (...args: Parameters<T>) => {
    if (!inThrottle) {
      fn(...args);
      inThrottle = true;
      setTimeout(() => {
        inThrottle = false;
      }, limit);
    }
  };
}

export function isExpired(date: Date): boolean {
  return new Date(date).getTime() < Date.now();
}

export function daysUntil(date: Date): number {
  const diff = new Date(date).getTime() - Date.now();
  return Math.ceil(diff / (1000 * 60 * 60 * 24));
}

export function daysBetween(date1: Date, date2: Date): number {
  const diff = Math.abs(new Date(date2).getTime() - new Date(date1).getTime());
  return Math.floor(diff / (1000 * 60 * 60 * 24));
}

export function addDays(date: Date, days: number): Date {
  const result = new Date(date);
  result.setDate(result.getDate() + days);
  return result;
}

export function parseWeightBarcode(
  barcode: string
): { weight: number; price: number; productCode: string } | null {
  if (barcode.length < 13) return null;
  const prefix = barcode.substring(0, 2);
  if (prefix !== "28" && prefix !== "29") return null;
  const productCode = barcode.substring(2, 7);
  const weightRaw = parseInt(barcode.substring(7, 12), 10);
  const weight = weightRaw / 1000;
  const priceRaw = parseInt(barcode.substring(12, 17) || "0", 10);
  const price = priceRaw / 100;

  if (isNaN(weight) || isNaN(price)) return null;
  return { weight, price, productCode };
}

export function formatPhoneNumber(phone: string, countryCode?: string): string {
  const cleaned = phone.replace(/\D/g, "");
  if (cleaned.length === 10) {
    return `(${cleaned.slice(0, 3)}) ${cleaned.slice(3, 6)}-${cleaned.slice(6, 10)}`;
  }
  if (cleaned.length === 11 && cleaned.startsWith("1")) {
    return `+1 (${cleaned.slice(1, 4)}) ${cleaned.slice(4, 7)}-${cleaned.slice(7, 11)}`;
  }
  if (countryCode && cleaned.startsWith(countryCode)) {
    const rest = cleaned.slice(countryCode.length);
    return `+${countryCode} ${rest}`;
  }
  return phone;
}

export function validateEmail(email: string): boolean {
  const re =
    /^[a-zA-Z0-9.!#$%&'*+/=?^_`{|}~-]+@[a-zA-Z0-9](?:[a-zA-Z0-9-]{0,61}[a-zA-Z0-9])?(?:\.[a-zA-Z0-9](?:[a-zA-Z0-9-]{0,61}[a-zA-Z0-9])?)*$/;
  return re.test(email);
}

export function validatePhone(phone: string): boolean {
  const cleaned = phone.replace(/\D/g, "");
  return cleaned.length >= 7 && cleaned.length <= 15;
}

export function validatePassword(password: string): { valid: boolean; message: string } {
  if (password.length < 8) {
    return { valid: false, message: "Password must be at least 8 characters long" };
  }
  if (!/[A-Z]/.test(password)) {
    return { valid: false, message: "Password must contain at least one uppercase letter" };
  }
  if (!/[a-z]/.test(password)) {
    return { valid: false, message: "Password must contain at least one lowercase letter" };
  }
  if (!/[0-9]/.test(password)) {
    return { valid: false, message: "Password must contain at least one number" };
  }
  if (!/[!@#$%^&*()_+\-=\[\]{};':"\\|,.<>\/?~`]/.test(password)) {
    return { valid: false, message: "Password must contain at least one special character" };
  }
  return { valid: true, message: "Password is valid" };
}

export function truncateText(text: string, maxLength: number): string {
  if (text.length <= maxLength) return text;
  return text.slice(0, maxLength - 3) + "...";
}

export function capitalizeFirst(text: string): string {
  if (!text) return text;
  return text.charAt(0).toUpperCase() + text.slice(1);
}

export function slugify(text: string): string {
  return text
    .toString()
    .toLowerCase()
    .trim()
    .replace(/\s+/g, "-")
    .replace(/[^\w-]+/g, "")
    .replace(/--+/g, "-")
    .replace(/^-+/, "")
    .replace(/-+$/, "");
}

export function generateRandomString(length: number): string {
  const chars = "ABCDEFGHIJKLMNOPQRSTUVWXYZabcdefghijklmnopqrstuvwxyz0123456789";
  let result = "";
  for (let i = 0; i < length; i++) {
    result += chars[Math.floor(Math.random() * chars.length)] ?? "";
  }
  return result;
}

export function hashString(str: string): string {
  let hash = 5381;
  for (let i = 0; i < str.length; i++) {
    hash = ((hash << 5) + hash + str.charCodeAt(i)) & 0xffffffff;
  }
  return (hash >>> 0).toString(16);
}

export function getTenantCacheKey(tenantId: string, key: string): string {
  return `tenant:${tenantId}:${key}`;
}

export function parseQueryString(queryString: string): Record<string, string> {
  const result: Record<string, string> = {};
  const qs = queryString.startsWith("?") ? queryString.slice(1) : queryString;
  if (!qs) return result;
  for (const pair of qs.split("&")) {
    const [key, value] = pair.split("=");
    if (key) {
      result[decodeURIComponent(key)] = value ? decodeURIComponent(value) : "";
    }
  }
  return result;
}

export function buildQueryString(
  params: Record<string, string | number | boolean | undefined>
): string {
  const parts: string[] = [];
  for (const [key, value] of Object.entries(params)) {
    if (value === undefined || value === null) continue;
    parts.push(`${encodeURIComponent(key)}=${encodeURIComponent(String(value))}`);
  }
  return parts.length > 0 ? `?${parts.join("&")}` : "";
}

export function sleep(ms: number): Promise<void> {
  return new Promise((resolve) => setTimeout(resolve, ms));
}

export async function retry<T>(
  fn: () => Promise<T>,
  maxRetries: number,
  delayMs: number
): Promise<T> {
  let lastError: unknown;
  for (let attempt = 0; attempt <= maxRetries; attempt++) {
    try {
      return await fn();
    } catch (error) {
      lastError = error;
      if (attempt < maxRetries) {
        await sleep(delayMs);
      }
    }
  }
  throw lastError;
}

export function formatFileSize(bytes: number): string {
  if (bytes === 0) return "0 Bytes";
  const units = ["Bytes", "KB", "MB", "GB", "TB", "PB"];
  const k = 1024;
  const i = Math.floor(Math.log(bytes) / Math.log(k));
  const size = parseFloat((bytes / Math.pow(k, i)).toFixed(2));
  return `${size} ${units[i] ?? "Bytes"}`;
}

export function getFileExtension(filename: string): string {
  const parts = filename.split(".");
  return parts.length > 1 ? (parts.pop() ?? "").toLowerCase() : "";
}

export function isImageFile(filename: string): boolean {
  const imageExtensions = ["jpg", "jpeg", "png", "gif", "bmp", "webp", "svg", "ico", "tiff", "avif"];
  const ext = getFileExtension(filename);
  return imageExtensions.includes(ext);
}

export function sanitizeFileName(filename: string): string {
  return filename
    .replace(/[<>:"/\\|?*\x00-\x1f]/g, "")
    .replace(/^\.+/, "")
    .replace(/\.+$/, "")
    .trim() || "untitled";
}

export function calculatePercentageChange(oldValue: number, newValue: number): number {
  if (oldValue === 0) return newValue === 0 ? 0 : 100;
  return roundToTwo(((newValue - oldValue) / Math.abs(oldValue)) * 100);
}

export function calculateGrowthRate(previous: number, current: number): number {
  if (previous === 0) return current === 0 ? 0 : 100;
  return roundToTwo(((current - previous) / previous) * 100);
}

export function calculateMargin(cost: number, sellingPrice: number): number {
  if (sellingPrice === 0) return 0;
  return roundToTwo(((sellingPrice - cost) / sellingPrice) * 100);
}

export function calculateMarkup(cost: number, sellingPrice: number): number {
  if (cost === 0) return 0;
  return roundToTwo(((sellingPrice - cost) / cost) * 100);
}

export function calculateSellingPrice(cost: number, marginPercent: number): number {
  if (marginPercent >= 100) return cost;
  return roundToTwo(cost / (1 - marginPercent / 100));
}

export function convertCurrency(amount: number, fromRate: number, toRate: number): number {
  if (fromRate === 0) return 0;
  return roundToTwo((amount / fromRate) * toRate);
}

export function getCurrentFiscalYear(): { start: Date; end: Date } {
  const now = new Date();
  const year = now.getMonth() >= 6 ? now.getFullYear() : now.getFullYear() - 1;
  const start = new Date(year, 6, 1);
  const end = new Date(year + 1, 5, 30);
  return { start, end };
}

export function getQuarter(date: Date): number {
  return Math.ceil((date.getMonth() + 1) / 3);
}

export function getMonthName(month: number, locale?: string): string {
  const d = new Date(2024, month - 1, 1);
  return d.toLocaleString(locale ?? "en-US", { month: "long" });
}

export function getWeekNumber(date: Date): number {
  const d = new Date(Date.UTC(date.getFullYear(), date.getMonth(), date.getDate()));
  const dayNum = d.getUTCDay() || 7;
  d.setUTCDate(d.getUTCDate() + 4 - dayNum);
  const yearStart = new Date(Date.UTC(d.getUTCFullYear(), 0, 1));
  return Math.ceil(((d.getTime() - yearStart.getTime()) / 86400000 + 1) / 7);
}

export function paginate<T>(
  items: T[],
  page: number,
  limit: number
): { items: T[]; meta: { total: number; page: number; limit: number; totalPages: number } } {
  const total = items.length;
  const totalPages = Math.max(1, Math.ceil(total / limit));
  const safePage = clamp(page, 1, totalPages);
  const start = (safePage - 1) * limit;
  const pagedItems = items.slice(start, start + limit);
  return {
    items: pagedItems,
    meta: { total, page: safePage, limit, totalPages },
  };
}

export function exportToCSV<T extends Record<string, any>>(data: T[], filename: string): void {
  if (data.length === 0) return;
  const headers = Object.keys(data[0] ?? {});
  const csvRows: string[] = [];
  csvRows.push(headers.map((h) => `"${h.replace(/"/g, '""')}"`).join(","));
  for (const row of data) {
    csvRows.push(
      headers
        .map((h) => {
          const val = row[h];
          const str = val === null || val === undefined ? "" : String(val);
          return `"${str.replace(/"/g, '""')}"`;
        })
        .join(",")
    );
  }
  const csvContent = csvRows.join("\n");
  const blob = new Blob([csvContent], { type: "text/csv;charset=utf-8;" });
  const url = URL.createObjectURL(blob);
  const link = document.createElement("a");
  link.setAttribute("href", url);
  link.setAttribute("download", `${filename}.csv`);
  link.style.display = "none";
  document.body.appendChild(link);
  link.click();
  document.body.removeChild(link);
  URL.revokeObjectURL(url);
}

export function arrayToChunks<T>(arr: T[], size: number): T[][] {
  const chunks: T[][] = [];
  for (let i = 0; i < arr.length; i += size) {
    chunks.push(arr.slice(i, i + size));
  }
  return chunks;
}

export function isValidJson(str: string): boolean {
  try {
    JSON.parse(str);
    return true;
  } catch {
    return false;
  }
}

export function deepClone<T>(obj: T): T {
  if (obj === null || typeof obj !== "object") return obj;
  if (obj instanceof Date) return new Date(obj.getTime()) as unknown as T;
  if (obj instanceof RegExp) return new RegExp(obj.source, obj.flags) as unknown as T;
  if (Array.isArray(obj)) return obj.map((item) => deepClone(item)) as unknown as T;
  const cloned: Record<string, unknown> = {};
  for (const key of Object.keys(obj as Record<string, unknown>)) {
    cloned[key] = deepClone((obj as Record<string, unknown>)[key]);
  }
  return cloned as T;
}

export function omitKeys<T extends Record<string, any>, K extends keyof T>(
  obj: T,
  keys: K[]
): Omit<T, K> {
  const result = { ...obj };
  for (const key of keys) {
    delete result[key];
  }
  return result as Omit<T, K>;
}

export function pickKeys<T extends Record<string, any>, K extends keyof T>(
  obj: T,
  keys: K[]
): Pick<T, K> {
  const result = {} as Pick<T, K>;
  for (const key of keys) {
    if (key in obj) {
      result[key] = obj[key];
    }
  }
  return result;
}

export function groupBy<T>(arr: T[], key: keyof T): Record<string, T[]> {
  const result: Record<string, T[]> = {};
  for (const item of arr) {
    const groupKey = String(item[key]);
    if (!result[groupKey]) {
      result[groupKey] = [];
    }
    result[groupKey]?.push(item);
  }
  return result;
}

export function sortBy<T>(arr: T[], key: keyof T, order: "asc" | "desc" = "asc"): T[] {
  const sorted = [...arr].sort((a, b) => {
    const aVal = a[key];
    const bVal = b[key];
    if (aVal < bVal) return -1;
    if (aVal > bVal) return 1;
    return 0;
  });
  return order === "desc" ? sorted.reverse() : sorted;
}

export function uniqueBy<T>(arr: T[], key: keyof T): T[] {
  const seen = new Set<unknown>();
  return arr.filter((item) => {
    const val = item[key];
    if (seen.has(val)) return false;
    seen.add(val);
    return true;
  });
}

const ARABIC_NUMBERS = ["٠", "١", "٢", "٣", "٤", "٥", "٦", "٧", "٨", "٩"];

export function arabicToEnglishNumbers(text: string): string {
  return text.replace(/[٠-٩]/g, (match) => {
    const idx = ARABIC_NUMBERS.indexOf(match);
    return idx !== -1 ? idx.toString() : match;
  });
}

export function englishToArabicNumbers(text: string): string {
  return text.replace(/[0-9]/g, (match) => {
    return ARABIC_NUMBERS[parseInt(match, 10)] ?? match;
  });
}

export function isRTL(locale: string): boolean {
  const rtlLocales = [
    "ar", "arc", "dv", "fa", "ha", "he", "khw", "ks", "ku", "ps", "ur", "yi",
    "az-Arab", "ff-Arab", "ks-Arab", "ku-Arab", "ms-Arab", "pa-Arab", "sd-Arab",
    "tk-Arab", "ug-Arab", "uz-Arab",
  ];
  const normalized = locale.toLowerCase().replace(/_/g, "-");
  return rtlLocales.some((rtl) => normalized.startsWith(rtl));
}

export function getBrowserLocale(): string {
  if (typeof navigator !== "undefined" && navigator.language) {
    return navigator.language;
  }
  return "en-US";
}

export function parseZatcaQR(qrData: string): {
  sellerName: string;
  vatNumber: string;
  timestamp: string;
  total: number;
  vat: number;
} {
  try {
    const decoded = atob(qrData);
    const parts = decoded.split("\x1D");
    const tags: Record<string, string> = {};

    let i = 0;
    while (i < decoded.length) {
      if (i + 1 >= decoded.length) break;
      const tag = decoded.charCodeAt(i).toString();
      i++;
      if (i >= decoded.length) break;
      const length = decoded.charCodeAt(i);
      i++;
      if (i + length > decoded.length) break;
      const value = decoded.substring(i, i + length);
      tags[tag] = value;
      i += length;
    }

    return {
      sellerName: tags["1"] ?? "",
      vatNumber: tags["2"] ?? "",
      timestamp: tags["3"] ?? "",
      total: parseFloat(tags["4"] ?? "0") || 0,
      vat: parseFloat(tags["5"] ?? "0") || 0,
    };
  } catch {
    return { sellerName: "", vatNumber: "", timestamp: "", total: 0, vat: 0 };
  }
}

export function generateZatcaQR(
  sellerName: string,
  vatNumber: string,
  timestamp: string,
  total: number,
  vat: number
): string {
  const encodeTLV = (tag: string, value: string): string => {
    return tag + String.fromCharCode(value.length) + value;
  };

  const parts = [
    encodeTLV("\x01", sellerName),
    encodeTLV("\x02", vatNumber),
    encodeTLV("\x03", timestamp),
    encodeTLV("\x04", total.toFixed(2)),
    encodeTLV("\x05", vat.toFixed(2)),
  ];

  return btoa(parts.join(""));
}